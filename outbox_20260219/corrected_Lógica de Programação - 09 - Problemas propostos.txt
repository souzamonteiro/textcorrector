uma nesta aula resolveremos alguns problemas propostos. O primeiro problema consiste em criar um programa que solicite valores ao usuário. Esses valores serão armazenados em duas variáveis. Em seguida, devemos determinar o menor deles e exibi-los na tela em ordem crescente. Tudo isso deve ser exibido no vídeo e tente resolver o problema.

Em seguida, retorne à exibição. Aqui está a resolução. No primeiro problema, nós criamos duas variáveis, a e b. Essas duas variáveis estão disponíveis aqui na paleta de variáveis. Também criamos uma variável de saída para apresentação dos resultados. As variáveis a e b recebem os valores digitados pelo usuário, ou seja, o primeiro e segundo valor, utilizando os blocos “set value”. Fornecemos um número porque vamos ter que fazer comparações numéricas. Utilizamos um bloco cinza onde foi adicionada a cláusula "se". Se o valor de a for maior que b, então é exibida a informação da redução dos valores em ordem crescente, colocando a primeira, seguida pelo valor de b. Observe que para isso foi criada uma variável de saída e essa variável de saída vai ser configurada com os dados das duas variáveis utilizando um bloco "criar texto".

Este bloco cria texto conforme foi utilizado porque precisamos concatenar os valores de ordem crescente, são o valor da variável a, expressão, e o valor da variável b, e o ponto final. Isso no caso de a é maior que b, caso contrário, a cláusula "senão" será executada, para definir a variável de saída para o valor de b, seguido do valor de a.

Utilizamos um bloco "criar texto" onde vertemos apenas as variáveis a e b, a que faltou colocar o bloco para apresentar os resultados do autor do crime, os colocar novamente um cristo, há de fato não é necessário, basta apresentar o resultado na variável de saída. Vamos executar o primeiro valor. Vou digitar o valor três, o segundo valor, o valor quatro. Os valores em ordem crescente são quatro, três.

Evite na verdade, em momentos críticos, que funcione em ordem crescente, do contrário, vamos tentar de novo. Valor três, halo quatro. Agora s três e quatro faltou dar o espaço que damos espaço do rei e depois do leigo, faz a mesma coisa que um date. Vamos lá. Três, enquanto os valores inteiros entre outros iguais, o inverter. Agora o coloca quatro, três, quatro e três. Veja aqui, ainda apresentam os valores em ordem crescente. Para salvar o resultado, basta clicar no botão, e o arquivo será baixado. Preço renomear o nome que desejar. É isso. O segundo problema consiste em criar uma função que receba um número inteiro, em seguida, calcule seu fatorial. O valor do fatorial deve ser exibido na tela. É importante lembrar que o fatorial de um número é calculado da seguinte forma: o valor do número vezes o valor do fatorial do número n menos um. Observe que como exemplo temos aqui o fatorial de cinco, que é o fatorial de cinco, ou seja, cinco vezes quatro vezes três vezes dois vezes um.

Em seguida, exiba esses valores na tela. Tente resolver o problema quando conseguir, volte à apresentação do vídeo e veja a solução. Neste caso, utilizamos o bloco "função" na aba "obter". Obtemos o bloco "função" da fonte, onde temos um plano para este bloco. Ele tem uma opção de ser renomeada. Forças os casos, recomendamos ela para "fatorial". E foi adicionada à variável n clicando na engrenagem e arrastando o bloco "nome de entrada" x e renomeando "nome variável nx". Finalmente, foi adicionado dentro do bloco "função" um bloco cinza para fazer a comparação "se". Se o valor de n for maior que zero, então o resultado é definido como n vezes o fatorial de n menos um. Caso contrário, o resultado é definido para o valor de zero.

A função fatorial é chamada recursivamente. Isso significa que a própria função é chamada várias vezes para processar parte dos dados. Quando o processo de cálculo fatorial é completo, o valor do fatorial é retornado. A função fatorial é então utilizada na função principal.