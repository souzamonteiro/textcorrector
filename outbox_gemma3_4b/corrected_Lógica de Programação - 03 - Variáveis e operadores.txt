Olá, nesta aula aprenderemos o que são variáveis e como utilizá-las com operadores. Na aula anterior, aprendemos a escrever uma expressão na tela utilizando o bloco imprime, que foi convertido para as diversas linguagens que estamos aprendendo neste curso, utilizando um conversor de linguagens que se encontra na parte inferior da janela do barco inflável. Nesta aula, aprenderemos a criar variáveis, utilizar operadores para fazer realizar operações matemáticas e lógicas com elas. Vamos criar variáveis. Para isso, vamos ao bloco “definir”, onde vamos criar a variável. Neste caso, vou criar a variável quando criamos uma variável, o sujeito na paleta de variáveis, os blocos definir qual o nome da variável falará e alterar o nome da variável para “Além”. Veja como utilizar os blocos de variáveis. Quando do bloco “definir” para a área de trabalho observamos que já é exibido na parte inferior. Para uma embalagem mais crítica, a igual a zero. Para levar a gente para, é criado primeiro “argola”, na única que, por estar quase morto, se fizermos a mesma coisa para observar o código da parte veremos que é criada uma variável utilizando o comando `var` seguido pelo nome da variável, com ponto e vírgula, e dentro da seção ou função, bem, mas veremos as duas eros terminando sempre com “Além”. Mesma coisa em Javascript, a igualação será utilizado para criar a variável e depois atribuído valor à variável, utilizando a igualdade, o operador de atribuição, igual a zero, terminando com ponto e vírgula. Em PHP, temos a mesma coisa, exceto que a variável, neste caso, é criada colocando o símbolo `$` antes do nome da variável. Este símbolo é o sinal de dólar, seguido de um dólar ponto e vírgula, depois para atribuir um valor à variável termos `$ igual a zero`. Em Portugal, semelhante a Javascript, da arte vallarta para criar a variável e depois artigo lazer. Então agora não implica a José Bento. Se quisermos agora definir um valor específico para a variável “Quarto”, vamos no bloco “matemática” e selecionamos o bloco que contém os números um, dois, três. Agora podemos atribuir por exemplo, observe que na área de código de atribuição da variável “a” mudou para igualar a “Além”. Isso vai acontecer em todas as linguagens de programação. A igualdade, um ponto e vírgula, em Lua, igual a “Além” é mais crítico agnaldo. Em PHP, `$ igual a` e guardá-lo em português. Se quisermos adora exibir o valor da variável na tela, utilizamos novamente o comando `imprimir`. Logo `imprimir`. Pode ou não conectá-lo abaixo do bloco novo do bloco `definir` e conectá-lo. E agora vou colocar nesse espaço onde temos aqui a PC o bloco com o nome da variável. Se clicarmos agora no botão executar, exibido o valor da variável “Além”. Observe como ficou o comando para exibir a variável “Além”. Veja como funciona. Observe fininha para um tornou a igualação imprime se tornou `print`. Se quisermos ver o mesmo comando cantar-te, observe `print` pode se reutilizar. Observe que a igualação imprime se tornou `print`. Se quisermos ver o mesmo comando funcionar, vamos executar. Observe que o valor de `a` é dois. Porque há um mais um ou dois se muitíssimo de modificarmos o operador para multiplicação e trocarmos o valor do operando à direita para três, executaremos teremos agora vamos mudar o valor de “b” também para três. Temos o valor dois e vamos agora multiplicar o valor de “b” por três, que é seis. Se ativá-lo dois vezes três é seis. Se quisermos realizar operações aninhadas, então se quisermos por exemplo, multiplicar o valor de “a” vezes outra operação matemática, podemos fazer também basta colocar no lugar deste espaço uma nova operação multiplicar, a praça matemáticas e colocar outro bloco arquitetos. Neste caso, vamos fazer com que “b” seja igual a “a” vezes três. Colocaremos o próprio valor de “a” à direita, como se fosse um parênteses. Assim, a expressão se torna `a * a`. O valor que será editada aqui vamos colocar três, e executaremos, que o resultado é 9. Observe que o bloco foi colocado inteiro à direita do operador, explicando que faz com que seja colocado um parênteses. Incluindo toda a operação que foi colocada no bloco no espaço à direita do operador. Vejamos o código, em que as duas variáveis “a” e “b” são criadas juntas utilizando o comando `var` (em Javascript) ou `var` (em outras linguagens) e atribuindo valor nome para elas. Em seguida, atribuímos o valor dois à variável “a” e o valor 3 à variável “b”. Dentro de parênteses, como já vimos, `a * a`, que resulta em 9. Em seguida, exibimos o valor de “b” na tela. Para concluir, a criação e utilização de variáveis com operadores numa linguagem de programação como a que estamos utilizando.