Olá! Nesta aula, vamos mergulhar no mundo das variáveis e aprender como utilizá-las com operadores. Na aula anterior, descobrimos como exibir expressões na tela usando o bloco “Imprime”, que foi adaptado para as diversas linguagens que estamos explorando neste curso. Esse conversor de linguagens está convenientemente localizado na parte inferior da janela do Barco Inflável.

Hoje, vamos nos concentrar na criação de variáveis e na aplicação de operadores para realizar operações matemáticas e lógicas com elas. Vamos começar criando átomos, que é onde vamos definir nossa variável. Ao criar uma variável, precisamos ir até a paleta de variáveis e selecionar o bloco “Definir”. Este bloco nos permite especificar o nome da nossa variável e, se necessário, alterá-lo posteriormente.

Vamos dar uma olhada em como usar os blocos de variáveis. Quando selecionamos o bloco “Definir” e o arrastamos para a área de trabalho, notamos que ele já é exibido na parte inferior da tela. Para uma variável mais simples, como “a”, podemos atribuir o valor “zero” a ela. Ao fazer isso, uma argola é criada na área de trabalho, representando a variável. Se repetirmos o mesmo processo, podemos observar o código gerado na parte inferior. O código mostra a criação da variável usando o comando “var”, seguido pelo nome da variável, um ponto e vírgula e, em seguida, a seção ou função onde a variável é definida. Essa sintaxe é consistente em várias linguagens de programação, incluindo JavaScript.

Em JavaScript, o comando “var” é usado para criar a variável, seguido pelo operador de atribuição “=“ para atribuir um valor a ela. O código termina com um ponto e vírgula. Em PHP, a sintaxe é semelhante, mas a variável é precedida por um símbolo de dólar “$” antes do nome da variável. Por exemplo, para criar uma variável chamada “a” e atribuir o valor “zero” a ela, usamos “$a = 0;”. Em Portugal, a sintaxe é semelhante à do JavaScript, mas usamos o comando “artigo” antes do nome da variável. Por exemplo, para criar uma variável chamada “a” e atribuir o valor “zero” a ela, usamos “artigo a = 0;”.

Agora que criamos nossa variável, podemos atribuir um valor específico a ela. Para fazer isso, vamos para o bloco “Matemática” e selecionar o bloco “Selecionar”. Este bloco nos permite escolher o valor que queremos atribuir à nossa variável.Vamos explorar como trabalhar com variáveis em programação. Variáveis são como caixas que armazenam informações, como números ou texto. Podemos dar nomes a essas caixas para nos referirmos a elas facilmente. Por exemplo, podemos criar uma variável chamada “a” e atribuir a ela o valor “um”. Em diferentes linguagens de programação, a sintaxe para atribuir valores às variáveis pode variar. Em JavaScript, usamos o sinal de igual (=), em Lua, também usamos o sinal de igual, mas em PHP, usamos o sinal de dólar ($). Em Portugal, o sinal de dólar também é usado.\n\nPara exibir o valor de uma variável na tela, usamos o comando “imprimir”. Podemos conectar o comando “imprimir” abaixo do bloco “definir” e especificar a variável que queremos exibir. Por exemplo, se quisermos exibir o valor da variável “a”, podemos colocar o nome da variável dentro do bloco “imprimir”. Quando executarmos o programa, o valor da variável “a” será exibido na tela.\n\nEm JavaScript, o comando para exibir uma variável é “window.alert()”. Em PHP, usamos o sinal de dólar ($) antes do nome da variável. Em Portugal, usamos o sinal de dólar ($) para atribuir valores às variáveis.\n\nPodemos realizar operações usando variáveis. Vamos criar outra variável chamada “b”. Agora temos um novo bloco e o bloco “definir b” também apareceu. Podemos renomear a variável “a” ou “b” a qualquer momento clicando na seta ao lado do nome da variável e selecionando a variável desejada. Podemos renomear ou excluir variáveis clicando nos respectivos botões.\n\nVamos definir a variável “a” para o número “um” e a variável “b” para o número “dois”. Podemos fazer isso usando a paleta de operações matemáticas e arrastando o bloco de operações matemáticas para o programa. Dentro do bloco de operações matemáticas, podemos especificar a operação que queremos realizar e as variáveis envolvidas. Por exemplo, podemos definir “a” para o resultado da soma de “a” e “b”. Quando executarmos o programa, o valor da variável “a” será atualizado para o resultado da soma.\n\nEm resumo, as variáveis são caixas que armazenam informações e podem ser usadas para realizar operações. Podemos atribuir valores às variáveis, exibir seus valores e realizar operações usando variáveis em diferentes linguagens de programação. Para garantir o valor, remova a variável e encaixe o bloco no espaço vazio dentro do bloco. Você pode trocar a operação matemática clicando na seta. Os operadores disponíveis são adição, subtração, multiplicação, divisão e potência (elevado a). Vamos usar a operação de adição, que é o padrão. Os operadores matemáticos geralmente têm operandos à esquerda e à direita. Neste caso, o operando à direita é um número. Se, em vez de imprimir o valor de “alarme”, mudarmos para “b”, veremos que o valor de “b” será o valor diário acrescido de um. Neste caso, o valor é indefinido. Ao clicar em “Executar”, o valor na trama é dois porque há um intervalo mais um, ou dois.

Se modificarmos o operador para multiplicação e trocarmos o valor do operando à direita para três, e executarmos, teremos o valor seis. Vamos mudar o valor de “a” também para três. Agora, temos o valor dois e vamos multiplicar o valor de “b” por três. Ao executar, o valor de “b” é seis. Podemos realizar operações aninhadas. Por exemplo, se quisermos multiplicar o valor diário por um e realizar outra operação matemática, podemos fazer isso colocando uma nova operação no lugar deste espaço. Vamos multiplicar “a” por três. O número que será editado aqui é três. Ao executar, o valor é dez porque dois vezes dois é quatro, e quatro mais três é sete. Observe que o bloco foi colocado inteiro à direita do operador de “a”. Na linguagem de blocos, isso faz com que seja colocado um parênteses, incluindo toda a operação que foi colocada no bloco no espaço à direita do operador. Vamos ver o código. Emparedou, escondeu, ficou. Da seguinte forma, são criadas as duas variáveis, a e b, atribuindo um valor a cada uma. Em seguida, a recebe o valor 2 e b recebe o valor 3. Dentro de parênteses, como eu disse, há mais três. Em seguida, o valor de b é exibido na tela. Vamos ver em outra linguagem. Opa, vamos ver o valor de a. A mesma operação em Dart: temos a igual a 2 e b igual a 3. Vamos ver em JavaScript. Eu adoro JavaScript, e ele sempre termina com ponto e vírgula. Em JavaScript, é necessário terminar sempre com ponto e vírgula. Observemos que as variáveis a e b estão sendo criadas juntas utilizando o comando var. Neste caso, como estão sendo criadas duas variáveis, os nomes das variáveis são separados por vírgula e sempre ao final, ponto e vírgula. É um comando JavaScript lindo, e termina com ponto e vírgula. É uma atribuição. Aguardou 2, o ponto e vírgula em seguida, e poderia igualar 3 vezes a mais 3, dentro de parênteses. Novamente, não é possível para exibição na tela ou em Dart, para b. Vamos ver em Lua. O código é semelhante, certo? Que não temos os pontos e vírgulas, ao passo que ao final das linhas, a desiguala 3 vezes a mais 3, e apresenta b. Mas é crítico, semelhante também. Adoro 2, degola 3 vezes a mais 3, e para exibição, system chama-se print. Em PHP, novamente, criamos as duas variáveis, a e b, e em seguida, $a e $b, terminando com ponto e vírgula. Três linhas de comando terminam com ponto e vírgula. $a, derrotou lá, vezes dentro de parênteses, $a mais 3, terminam com ponto e vírgula. Print idolatre. Em Python, semelhante às outras linguagens, e pai, mas já ficamos. Existem muitos operadores matemáticos disponíveis na paleta de matemática. Nós temos analisado as funções. Neste caso, aqui está destacado raiz quadrada, mas podemos utilizar qualquer uma das outras funções disponíveis. Vou deletar os blocos do palco. Selecionar o bloco mais externo e delicado. Ela deleta todos os blocos. Apago todos os blocos. Vou colocar agora o bloco de raiz quadrada, e vamos operar com a variável a. Vou colocar o número que dizem ser feliz para o valor de a, e se pedirmos… Agora que definimos b como a raiz quadrada de dirá, obtemos o resultado quatro, pois quatro ao quadrado é igual a dirá. Podemos aplicar esse conceito a várias funções. Por exemplo, a função valor absoluto retorna um valor positivo, independentemente de o número ser negativo. O logaritmo neperiano e o logaritmo na base dez são outras funções. A exponenciação envolve elevar dez ao valor de dirá.

Além dessas funções, temos funções trigonométricas como seno, cosseno e tangente. Observe que, ao selecionar a função seno, podemos definir o valor de dirá até quarenta e cinco. Ao executar o programa, obtemos o valor cento e quarenta e cinco. Observe que todas as outras funções transcendentais, como cosseno e tangente, são sempre cotadas para cosseno. O arco tangente também está disponível.

Temos algumas constantes, como p. Podemos excluir o bloco de constantes e adicionar o bloco de constantes novamente. Outras constantes incluem p, o número, a raiz quadrada de dois, uma constante, a raiz quadrada de um, mei infinito e <UNK>. Temos uma função especial para verificar se um determinado número é par. Por exemplo, vamos colocar aqui nota e excluir isso. Se colocarmos a variável aqui e um valor par, como quatro, e pedirmos para definir bem, obteremos o valor correspondente a verdadeiro ou falso. Se o número for par, será verdadeiro; se não for, será falso. Se aplicarmos as ações, veremos que o monstro parecia que estrutura o inglês é verdadeiro. No entanto, se colocarmos três, será falso porque três não é um número par.

Existem outros blocos, como blocos com funções de arredondamento, somar uma lista, divisão e assim por diante. Esses são os blocos de operações matemáticas. Eles também permitem realizar operações com funções matemáticas, mas com menos opções disponíveis para cada função operacional. Para cada uma dessas operações matemáticas que realizamos, geramos um código correspondente em uma linguagem de programação. Observe que verificar se a reparar na linguagem pai corresponde a fazer a ele esse símbolo especial que foi exibido aqui. Percentagem impacto significa resto da divisão diária por dores. Para comparar o valor de um número com zero, podemos usar a operação de divisão por dois e verificar se o resto é zero. Em JavaScript, essa operação é bastante semelhante à de PHP. No PHP, usamos o operador “%” para obter o resto da divisão por dois, enquanto no JavaScript usamos o operador “%”. Vale ressaltar que os programadores em PHP geralmente colocam um dólar na frente da variável, mas a operação em si é muito semelhante.

Em relação às funções matemáticas, a maioria das linguagens de programação as define da mesma forma. Por exemplo, a função seno é representada como “sin” em JavaScript e “sin” em PHP. No entanto, para usar essas funções matemáticas especiais, precisamos importar uma biblioteca que contém suas definições. Em JavaScript, importamos a biblioteca “math” usando a instrução “import math”. Essa biblioteca contém as definições das funções seno, cosseno e tangente, entre outras.

Depois de importar a biblioteca, podemos usar as funções matemáticas precedendo seus nomes com o prefixo “math.”. Por exemplo, para usar a função seno, usamos “math.sin()”. Em JavaScript, também podemos usar o operador “rad” para converter graus em radianos, o que é necessário para algumas funções matemáticas, como a função seno. Precisamos converter esses cinco graus decimais para radianos porque as funções matemáticas foram projetadas para trabalhar com números em radianos. Para converter graus para radianos, usamos a função “toRadians” do Mercator. Para converter radianos para graus, realizamos um cálculo dividindo o valor da variável por 180 e multiplicando pelo valor de π. O valor de π é uma constante definida na biblioteca principal, por isso o colocamos nesse ponto. Se quisermos ver o mesmo código em JavaScript, notaremos que ele também usa “toRadians” dividindo por 180 e multiplicando por π. No entanto, ele critica o mesmo ponto, dividindo por 180 e multiplicando por π. Observe que algumas linguagens de programação, como Java e Python, referenciam variáveis com a primeira letra maiúscula, enquanto outras, como JavaScript e PHP, usam a primeira letra minúscula. Cada linguagem de programação tem suas próprias peculiaridades. Em Java e Python, as variáveis sempre começam com a primeira letra maiúscula, enquanto em JavaScript e PHP, elas sempre começam com a primeira letra minúscula. Em PHP, não é necessário usar nenhum prefixo. Prefixos são nomes de objetos especiais, incluindo bibliotecas. Em PHP, não precisamos usar nenhum prefixo. Já vimos bastante sobre a biblioteca “math” aqui. Ela é importada usando “import math”. Essa biblioteca também é necessária para a biblioteca “portableLottery”. Importamos “portableLottery” usando “import portableLottery”. Em seguida, usamos a função “sin” da biblioteca “math”. O nome da função é precedido pelo nome da biblioteca e um ponto. Uma constante é uma variável cujo valor não muda. Chamamos essas variáveis de constantes.