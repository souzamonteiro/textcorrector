Olá! Nesta aula, vamos nos aprofundar nas operações lógicas relacionais. Na aula anterior, aprendemos sobre operadores matemáticos e como usá-los com variáveis para realizar operações de backup. Também vimos como essas operações são descritas nas principais linguagens de programação. Nesta aula, vamos expandir nosso estudo de operadores para incluir operadores lógicos e relacionais.

Os operadores lógicos são usados para determinar se dois valores são iguais ou não. Por exemplo, podemos usá-los para decidir se podemos continuar a execução de um programa ou saltar para outro local com base na solução dessas operações. Essas operações determinam se cinco proposições são verdadeiras ou falsas. Embora estejamos acostumados a trabalhar com proposições em nosso dia a dia, geralmente o fazemos de forma informal. No entanto, nosso cérebro é capaz de raciocinar a partir de proposições.

Vamos ver como essas proposições podem ser construídas usando operadores matemáticos e marcos lógicos. Vou limpar este código e apresentar a paleta de lógica. Na paleta de lógica, temos algumas construções que veremos na próxima aula. As mais importantes são os operadores relacionais, que estão neste bloco, e os operadores lógicos, que estão no segundo bloco com a letra “i”.

Vamos construir duas variáveis, “a” e “b”. Vamos definir os valores de “a” e “b”. Em seguida, vamos criar um bloco e tentar usar operadores lógicos para “a” e “b”. O resultado desses operadores será sempre um valor verdadeiro ou falso, dependendo de como eles operam com os valores à esquerda ou direita do operador.

Vamos criar mais uma variável, como sempre fazemos no Scratch. Vamos ver a variável “central” e o resultado da operação. Vamos realizar operações com as variáveis ‘a’ e ‘b’. Primeiro, vamos pegar o bloco lógico ‘p’ e colocá-lo na caixa. Vamos trabalhar com as variáveis ‘arrebenta’ e encaixar os valores de ‘<UNK>’ à esquerda e à direita de ‘preto’. Observe a operação lógica ‘e’. Ela retornará um valor verdadeiro se o valor à esquerda da tela for igual ao valor à direita dela, e ambos forem verdadeiros. Em computação, o valor ‘um’ é considerado verdadeiro, enquanto o valor ‘zero’ é considerado falso. Este operador ‘e’ retornará como resultado verdadeiro somente se ambos os operandos forem verdadeiros. Neste caso, ‘a’ e ‘b’ estão no mesmo intervalo e são sempre considerados verdadeiros.

Agora, vamos colocar o bloco ‘imprimir’ para exibir o valor ‘desastre’. Vamos executar o código. O valor ‘um’ foi considerado verdadeiro. Vamos agora alterar o valor de uma das variáveis para execução. Vamos alterar a variável ‘a’. Neste caso, ‘a’ será considerado verdadeiro, enquanto ‘b’ será considerado falso. O operador ‘e’ só retorna verdadeiro se ambos forem verdadeiros. Basta que um deles seja falso e ele não retornará verdadeiro. Ele retornará zero, que significa falso.

Vamos inverter os valores. Vamos colocar o valor do primeiro operando como zero e o valor do segundo operando como verdadeiro. Executando o código, também temos zero porque basta que um dos valores seja falso e o resultado será zero, que significa falso. Temos um segundo operador aqui, o operador ‘ou’. Ele funciona de forma diferente. O operador ‘ou’ retornará verdadeiro se qualquer um dos dois valores for verdadeiro. Se qualquer um dos valores for verdadeiro, ele retornará verdadeiro. Neste caso, ‘a’ é verdadeiro e ‘b’ é verdadeiro. Ele retornou verdadeiro.

Vamos inverter os valores. ‘b’ se tornou zero e ‘a’ também se tornou zero. O operador ‘ou’ retornou verdadeiro. Vamos agora colocar os dois valores como verdadeiros. Não só verdadeiro porque basta que um dos dois seja verdadeiro. Agora, vamos colocar os nomes como falsos. O operador ‘ou’ retorna falso se ambos os valores forem falsos. Se qualquer um dos valores for verdadeiro, ele retorna verdadeiro. Se ambos os valores forem falsos, ele retorna falso. Essas duas operações lógicas são suficientes para começar. Vamos ver como elas funcionam.Operações lógicas são encontradas em todas as linguagens de programação que abordaremos. Isso não é apenas para demonstrar sua presença, mas também para destacar sua consistência em várias linguagens. Essas operações envolvem a atribuição de valores a variáveis e a realização de comparações entre elas. A operação de igualdade é representada por dois pontos iguais (==) em vez da palavra “igual”. Essa notação é comum em muitas linguagens de programação, incluindo PHP, Portugal e Impacto. Já vimos a operação de igualdade (==) em PHP, Portugal e Impacto, e também vimos a operação de igualdade (==) em Cintia. O operador de igualdade (==) em JavaScript é representado por dois pontos iguais (==), enquanto em Lua é representado por dois pontos iguais (==). Em PHP, Portugal e Impacto, o operador de igualdade (==) é representado por dois pontos iguais (==).

Vamos agora aprender como usar operações relacionais, que são usadas para comparar valores. Podemos adicionar um bloco de operações relacionais e colocar as variáveis a e b à esquerda e à direita, respectivamente. Neste caso, estamos comparando os dois valores usando o operador de igualdade (==). No entanto, também existem outros operadores de comparação, como diferente de (!=), menor que (<), menor ou igual (<=), maior que (>) e maior ou igual (>=).

Vamos executar algumas dessas operações. Quando usamos o operador de igualdade (==), obtemos falso porque a não é igual a b. Quando usamos o operador diferente de (!=), obtemos verdadeiro porque a é diferente de b. Quando usamos o operador menor que (<), obtemos falso porque a não é menor que b. Quando usamos o operador menor ou igual (<=), também obtemos falso. No entanto, quando usamos o operador maior que (>), obtemos verdadeiro porque a é maior que b. O operador maior ou igual (>=) também retornará verdadeiro.

Vamos ver como essas operações aparecem em diferentes linguagens de programação. O operador maior ou igual (>=) é representado pelo sinal de maior do teclado seguido pelo sinal de igual do teclado em PHP, Portugal e Impacto. Em JavaScript, é o mesmo operador maior seguido por outro igual. Em Lua, também é o operador maior seguido por outro igual.A única diferença significativa entre eles é a presença do dólar antes dos nomes de outras variáveis. Isso é sempre assim em Portugal. Também temos “menor”, “maior”, “igual” e “pai”, que nunca tínhamos visto antes. Vejamos um operador diferente neste caso. Ele impacta a operação de forma diferente, representada pelo sinal de exclamação seguido de igual. Além disso, “imperador” em JavaScript e “imperador” em Lua são um sinal duplo seguido de igual, mas “crítico” é um sinal duplo seguido de exclamação igual. “HP” também é “Portugal” também, mas já vimos isso. Vejamos o operador “igual”. Ele é um duplo operador igual que compara. O operador igual sozinho é atribuição. Quando quero comparar, reutilizo o operador igual. Em JavaScript, “igual” também é um duplo operador, e em Lua também, com dupla barra. “Crítico” também é “igual” em PHP, “Portugal” também, “gradual” também e “para” também. Vejamos o operador “menor igual”. É a mesma coisa que o operador “maior igual”. É simplesmente o sinal de menor seguido de igual. Ele impacta “encarte” em JavaScript, “luanda” em Lua e é mais crítico em PHP e “Portugal”.